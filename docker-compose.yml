# docker-compose.yaml

services:
  build-linux:
    build:
      dockerfile: ./Dockerfile
      context: .
    volumes:
      #- .:/app
      - tauriRelease:/app/src-tauri/target/release
    working_dir: /app
    platform: linux/amd64
    environment:
      TAURI_PRIVATE_KEY: ${TAURI_PRIVATE_KEY:-secret}
      TAURI_KEY_PASSWORD: ${TAURI_KEY_PASSWORD:-secret}
      #APPLE_SIGNING_IDENTITY: ${APPLE_SIGNING_IDENTITY}
    #command: 'cargo tauri build --target x86_64-unknown-linux-gnu'

  #build-linux-arm:
  #  build:
  #    dockerfile: ./build/linux/dockerfile_arm
  #    context: .
  #  volumes:
  #    - .:/app
  #  working_dir: /app
  #  environment:
  #    TAURI_PRIVATE_KEY: ${TAURI_PRIVATE_KEY}
  #    TAURI_KEY_PASSWORD: ${TAURI_KEY_PASSWORD}
  #    APPLE_SIGNING_IDENTITY: ${APPLE_SIGNING_IDENTITY}
  #  command: 'cargo tauri build --target aarch64-unknown-linux-gnu'

volumes:
  tauriRelease:
